# =============================================================================
# Compliance Service — Python Dependencies
# =============================================================================
# PII detection (spaCy NLP + regex), regulatory compliance verification
# (GDPR, HIPAA, CCPA), tamper-evident compliance certification, and
# audit logging for the Synthetic ERP Data Generation Platform.
#
# Install: pip install -r requirements.txt
#
# NOTE: The spaCy language model (en_core_web_sm) is NOT listed here.
#       It is downloaded separately in the Dockerfile via:
#         python -m spacy download en_core_web_sm
# =============================================================================

# -----------------------------------------------------------------------------
# Core Framework
# -----------------------------------------------------------------------------
flask>=3.1.0,<3.2.0
flask-restful>=0.3.10,<0.4.0
flask-cors>=4.0.0,<5.0.0
gunicorn>=21.2.0,<22.0.0
pydantic>=2.6.0,<3.0.0

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
pymongo>=4.6.0,<5.0.0
redis>=5.0.0,<6.0.0

# -----------------------------------------------------------------------------
# PII Detection / NLP
# -----------------------------------------------------------------------------
spacy>=3.7.0,<3.8.0

# -----------------------------------------------------------------------------
# Regulatory Compliance / Cryptography
# -----------------------------------------------------------------------------
python-jose[cryptography]>=3.3.0,<4.0.0
cryptography>=42.0.0,<43.0.0

# -----------------------------------------------------------------------------
# Observability
# -----------------------------------------------------------------------------
structlog>=24.1.0,<25.0.0
opentelemetry-api>=1.22.0,<2.0.0
opentelemetry-sdk>=1.22.0,<2.0.0
opentelemetry-instrumentation-flask>=0.43b0
prometheus-client>=0.20.0,<1.0.0

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
circuitbreaker>=2.0.0,<3.0.0

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0
mongomock>=4.1.0,<5.0.0
ruff>=0.4.0
mypy>=1.10.0
